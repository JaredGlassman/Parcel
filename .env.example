# ── Required ─────────────────────────────────────────────────────────────────
# Get from: https://console.anthropic.com/
# Used for: satellite image analysis (fencing, pool, solar detection)
ANTHROPIC_API_KEY=sk-ant-api03-...

# ── Optional — better satellite imagery ──────────────────────────────────────
# Get from: https://console.cloud.google.com/ → Maps Static API
# Without this, free ESRI imagery is used (works fine, slightly lower resolution)
GOOGLE_MAPS_API_KEY=AIzaSy...

# ── Stripe (paid lead purchases + zip exclusivity) ───────────────────────────
STRIPE_SECRET_KEY=sk_live_...

# ── CRM password gate (for /api/crm dashboard) ───────────────────────────────
CRM_PASSWORD=changeme

# ── Google Sheet sink — "Parcel" sheet in info@goodguyspools.com Drive ───────
# Lead capture writes signups + delivered leads here. The sheet has two tabs:
# "Signups" and "Leads Delivered".
PARCEL_SHEET_ID=10t2daGVenpFHh6BoOnHHAnMGJH1V5lCtskcNa5QFuyI

# Reuse the same OAuth client + refresh token from the AI Leads project.
# Refresh token is for info@goodguyspools.com with Sheets scope.
GOOGLE_CLIENT_ID=...apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-...
SHEETS_REFRESH_TOKEN_INFO=1//...
